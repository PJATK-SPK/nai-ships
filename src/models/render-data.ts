import { RenderGridData } from "./render-grid-data";
import { RenderTextData } from "./render-text-data";

export class RenderData {
    grid: RenderGridData | undefined;
    text: RenderTextData | undefined;
}
